apiVersion: v1
kind: Service
metadata:
  name: guardrails
  labels:
    app.kubernetes.io/name: guardrails
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: guardrails
  ports:
    - name: http
      port: 8000
      targetPort: http
