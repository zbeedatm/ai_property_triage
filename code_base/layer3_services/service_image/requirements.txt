fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic>=2.9,<3

# PyTorch CPU-only (significantly smaller than CUDA build)
torch==2.3.0+cpu
torchvision==0.18.0+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Image handling
Pillow==10.3.0
httpx==0.27.0          # fetch images from URLs

# Utilities
python-dotenv==1.0.1
numpy==1.26.4
