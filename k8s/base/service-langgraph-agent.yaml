apiVersion: v1
kind: Service
metadata:
  name: langgraph-agent
  labels:
    app.kubernetes.io/name: langgraph-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: langgraph-agent
  ports:
    - name: http
      port: 8000
      targetPort: http
